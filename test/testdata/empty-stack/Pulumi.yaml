name: empty-stack
runtime: yaml
description: A YAML program that does very little.
configuration:
  "aws:region": { type: 'String' }
outputs:
  region: ${aws:region}
  notSoSecret: "safe"
  secretVal:
    Fn::Secret: { "val": "very secret" }
  nestedSecret:
    plain: "foo"
    secret:
      Fn::Secret: "bar"
